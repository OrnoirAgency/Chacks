<resources>


    <!-- Base application theme. values-night.xml -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:fontFamily">@font/abel</item>

    </style>

    <style name="MaterialSearchViewStyle">
        <!-- Background for the search bar -->
        <item name="searchBackground">@color/background</item>

        <!-- Change voice icon -->
        <item name="searchVoiceIcon">@drawable/ic_microphon_icon_1</item>

        <!-- Change clear text icon -->
        <item name="searchCloseIcon">@drawable/ic_close_icon_1</item>

        <!-- Change up icon -->
        <item name="searchBackIcon">@drawable/ic_back_icone</item>

        <!-- Change icon for the suggestions -->
        <item name="searchSuggestionIcon">@drawable/ic_suggestion</item>

        <!-- Change background for the suggestions list view -->
        <item name="searchSuggestionBackground">@color/icon_checked</item>

        <!-- Change text color for edit text. This will also be the color of the cursor -->
        <item name="android:textColor">@color/textColorPrimary</item>

        <!-- Change hint text color for edit text -->
        <item name="android:textColorHint">@color/selection</item>

        <!-- Hint for edit text -->
        <item name="android:hint">@string/search_hint</item>
    </style>

    <style name="MyDialog" parent="Theme.AppCompat.DayNight.Dialog.Alert"/>

    <style name="MySwitch">
        <item name="colorControlActivated">@color/switchColor</item>
    </style>

</resources>
